# 免费学科测评体系 — 系统设计文档

> 版本: 1.0 | 日期: 2026-02-21 | 状态: 设计阶段

## 一、产品定位与核心目标

### 1.1 定位

**免费学科测评** 是 BasisPilot 的**漏斗顶部获客引擎**——面向准贝赛思学生/在读学生/家长，提供免费、专业、个性化的学科能力诊断，构建从 "免费测评 → 查看报告 → 注册建档 → 体验辅导 → 付费转化" 的完整链路。

核心公式：**免费测评(60%注册转化) → 诊断报告(50%体验课) → 短期课包(70%续费) → 长期签约**

### 1.2 战略价值

| 维度 | 价值 |
|------|------|
| **获客** | 零门槛体验产品核心能力，降低家长决策阻力 |
| **建档** | 测评即注册，自然采集学生画像（年级/校区/学科水平） |
| **诊断** | 精准定位学科薄弱环节，为后续辅导提供数据基础 |
| **转化** | 报告中嵌入个性化辅导建议 + 限时体验课引导 |
| **传播** | 家长分享测评报告/成绩到家长群 → 裂变获客 |
| **数据** | 积累题目难度数据、学生能力分布数据，持续优化 |

### 1.3 目标用户

| 用户类型 | 场景 | 测评类型 |
|----------|------|----------|
| **准新生家长** | 孩子准备入学 BASIS，想知道水平差距 | 入学能力预评估 |
| **在读学生家长** | Benchmark 后发现成绩下滑，想定位问题 | 学科诊断测评 |
| **在读学生** | 自主检测学科掌握程度 | 章节/单元测试 |
| **教育顾问** | 初次咨询时快速了解学生水平 | 快速摸底测评 |

---

## 二、测评体系设计（教育维度）

### 2.1 测评分类

```
免费学科测评
├── 📋 入学能力预评估 (Pre-Admission Assessment)
│   ├── 数学水平测试 (Math Placement)
│   ├── 英语能力测试 (English Proficiency)
│   └── 综合学术素养 (Academic Readiness)
│
├── 📊 学科诊断测评 (Subject Diagnostic)
│   ├── 数学诊断 (Math Diagnostic)
│   ├── 物理诊断 (Physics Diagnostic)
│   ├── 化学诊断 (Chemistry Diagnostic)
│   ├── 生物诊断 (Biology Diagnostic)
│   ├── ELA 诊断 (English Language Arts)
│   └── 历史诊断 (History)
│
└── ⚡ 快速摸底 (Quick Assessment)
    └── 5分钟全科速测 (5-min Multi-Subject)
```

### 2.2 BASIS 课程对齐（按年级）

BASIS 课程超前同等学校 1-2 年。测评内容必须精准对齐 BASIS 各年级实际教学进度：

| BASIS 年级 | 数学 | 科学 | ELA |
|-----------|------|------|-----|
| **G5** | Pre-Algebra | Intro Chemistry + Physics + Biology (分科开始) | Narrative + Expository Writing |
| **G6** | Algebra I | Chemistry I + Physics I + Biology I | Literary Analysis |
| **G7** | Geometry | Chemistry II + Physics II + Biology II | Persuasive Essay |
| **G8** | Algebra II / Pre-Calc | Adv. Chemistry + Physics + Biology | Research Paper |
| **G9** | Pre-Calculus / AP Calc AB | AP Biology / AP Hum. Geo. (首批 AP) | AP-level Reading |
| **G10** | AP Calculus AB/BC | AP Chemistry / AP Physics 1 | AP English Language |
| **G11** | AP Calculus BC / AP Stats | AP Physics C / AP CS A | AP English Literature |
| **G12** | Post-AP / Research | Post-AP / Research | Post-AP / Senior Thesis |

### 2.3 题目类型与评估维度

#### 数学评估维度

| 维度 | 权重 | 题目类型 | 说明 |
|------|------|----------|------|
| **概念理解** | 30% | 选择题 + 判断题 | 核心定义和性质 |
| **计算能力** | 25% | 填空题 + 计算题 | 运算准确性和速度 |
| **问题解决** | 25% | 应用题(Word Problems) | 英文表述 → 数学建模 |
| **推理论证** | 20% | 开放题(Agent 评估) | 多步推理、证明思路 |

#### 英语评估维度

| 维度 | 权重 | 题目类型 | 说明 |
|------|------|----------|------|
| **词汇量** | 20% | 选择题(Tier 2+3 词汇) | 学术词汇掌握度 |
| **阅读理解** | 30% | 段落理解+推断题 | 学术文本、教材节选 |
| **语法用法** | 20% | 改错+选择 | 学术写作语法 |
| **写作能力** | 30% | 短文写作(Agent 评估) | Paragraph → Essay |

#### 科学评估维度

| 维度 | 权重 | 题目类型 | 说明 |
|------|------|----------|------|
| **知识掌握** | 30% | 选择题 | 核心概念和事实 |
| **实验设计** | 20% | 场景题 | 变量识别、实验步骤 |
| **数据分析** | 25% | 图表解读题 | 数据提取和趋势判断 |
| **科学推理** | 25% | 开放题(Agent 评估) | 假设→实验→结论 |

### 2.4 自适应出题算法（规则引擎）

```
核心原则：CAT (Computer Adaptive Testing) 简化版

1. 初始题目 → 中等难度 (difficulty = 0.5)
2. 答对 → 下一题难度 +0.15
   答错 → 下一题难度 -0.15
3. 连续答对 2 题 → 难度 +0.25 (加速上升)
   连续答错 2 题 → 难度 -0.25 (加速下降)
4. 达到难度天花板/地板 → 转换到其他知识点
5. 最终能力值 = 最后 5 题难度均值 × 调整系数

难度分级:
  0.0-0.2  → 基础 (Below Grade Level)
  0.2-0.4  → 发展中 (Approaching Grade Level)
  0.4-0.6  → 达标 (At Grade Level)
  0.6-0.8  → 优秀 (Above Grade Level)
  0.8-1.0  → 卓越 (Advanced / AP Ready)
```

### 2.5 题库规模规划

| 学科 | 年级范围 | 每年级题量 | 总题量 | MVP 首批 |
|------|---------|-----------|--------|---------|
| 数学 | G5-G12 | 80 题 | 640 | 200 (G5-G8) |
| 英语 | G5-G12 | 60 题 | 480 | 150 (G5-G8) |
| 物理 | G6-G12 | 50 题 | 350 | 100 (G6-G8) |
| 化学 | G6-G12 | 50 题 | 350 | 100 (G6-G8) |
| 生物 | G6-G12 | 50 题 | 350 | 100 (G6-G8) |
| **合计** | | | **2,170** | **650** |

---

## 三、用户全流程设计

### 3.1 完整用户旅程

```
┌─────────────────────────────────────────────────────────────────────┐
│                        用户旅程总览                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1. 触达          2. 落地页         3. 信息采集       4. 测评         │
│  ─────────────    ─────────────    ─────────────    ─────────────    │
│  家长群分享       免费测评入口      年级/校区/学科    自适应答题        │
│  朋友圈海报       价值承诺          手机号注册        实时进度条        │
│  公众号文章       选择测评类型      (创建学生档案)    计时器           │
│  顾问推荐链接                                                        │
│                                                                     │
│  5. 报告生成       6. 报告查看       7. 引导转化       8. 持续跟进    │
│  ─────────────    ─────────────    ─────────────    ─────────────    │
│  规则评分          雷达图/成绩       个性化辅导建议    学生档案沉淀     │
│  + Agent 分析      知识点分析        限时体验课       定期推送复测     │
│  → 生成报告        年级对齐评价      课包推荐         学习进度追踪     │
│                   强项/弱项                                          │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 关键转化节点

| 节点 | 触发条件 | 转化动作 | 预期转化率 |
|------|---------|---------|-----------|
| **落地页→开始测评** | 点击"开始免费测评" | 无门槛开始 | 70% |
| **测评中→完成测评** | 答题进度 > 60% | 进度激励 + 已答题不浪费 | 85% |
| **完成→注册查看报告** | 测评完成 | 手机号注册 → 查看完整报告 | 60% |
| **报告→体验课** | 查看报告中的薄弱环节 | "针对性体验课" CTA | 30% |
| **体验课→课包** | 体验课结束 | 限时优惠课包 | 50% |

### 3.3 报告分享裂变

```
报告页 → "分享给好友也来测一测" 按钮
         ├── 生成海报 (学科雷达图 + 二维码)
         ├── 复制链接 (含 referral code)
         └── 微信分享卡片

裂变奖励：
  - 邀请 1 人完成测评 → 解锁完整报告 (如果是 Freemium 模式)
  - 邀请 3 人 → 赠送 1 节体验课
  - 邀请 5 人 → 解锁 "AP 选课规划" 专属功能
```

---

## 四、技术架构设计

### 4.1 系统架构总览

```
┌──────────────────────────────────────────────────────────────────────┐
│                        Frontend (Next.js)                            │
│  ┌────────────┐ ┌──────────────┐ ┌────────────┐ ┌───────────────┐  │
│  │ 测评落地页  │ │  答题界面     │ │  报告页面   │ │ 学生档案面板  │  │
│  │ /assessment │ │ /assessment/ │ │ /assessment/│ │ /profile      │  │
│  │             │ │   [id]/take  │ │  [id]/report│ │               │  │
│  └─────┬──────┘ └──────┬───────┘ └─────┬──────┘ └───────┬───────┘  │
│        │               │               │                │           │
│  ──────┼───────────────┼───────────────┼────────────────┼─────────  │
│        └───────────────┴───────┬───────┴────────────────┘           │
│                                │ REST API                           │
└────────────────────────────────┼────────────────────────────────────┘
                                 │
┌────────────────────────────────┼────────────────────────────────────┐
│                    Business API (FastAPI :5096)                      │
│  ┌─────────────┐ ┌────────────┐ ┌───────────────┐ ┌─────────────┐ │
│  │ Assessment   │ │  Question  │ │  Scoring      │ │  Report     │ │
│  │ Controller   │ │  Engine    │ │  Engine       │ │  Generator  │ │
│  │              │ │ (规则引擎)  │ │ (规则+Agent)  │ │ (Agent)     │ │
│  └──────┬──────┘ └─────┬──────┘ └──────┬────────┘ └──────┬──────┘ │
│         │              │               │                 │         │
│  ───────┼──────────────┼───────────────┼─────────────────┼───────  │
│         └──────────────┴───────┬───────┴─────────────────┘         │
│                                │                                    │
│  ┌─────────────┐  ┌───────────┴──────────┐  ┌──────────────────┐  │
│  │ PostgreSQL   │  │  LangGraph Agent     │  │  Mem0 / Qdrant   │  │
│  │ (题库+结果)  │  │  (开放题评估+报告)    │  │  (用户记忆)       │  │
│  └─────────────┘  └──────────────────────┘  └──────────────────┘  │
└────────────────────────────────────────────────────────────────────┘
```

### 4.2 规则引擎 vs Agent 的分工

**核心原则：选择题用规则，开放题用 Agent，报告用 Agent + 模板**

| 环节 | 引擎 | 原因 |
|------|------|------|
| **出题选择** | 规则引擎 | 自适应算法需确定性，不能靠 LLM 随机选 |
| **选择题评分** | 规则引擎 | 标准答案，毫秒级响应 |
| **填空题评分** | 规则引擎 | 标准答案 + 模糊匹配 |
| **计算题评分** | 规则引擎 + Agent | 规则匹配最终答案 + Agent 评估过程步骤 |
| **写作题评分** | Agent | 需要语义理解，参考 rubric 打分 |
| **实验设计题** | Agent | 需要评估逻辑合理性 |
| **生成诊断报告** | Agent + 模板 | 模板结构 + Agent 填充个性化分析 |
| **学习建议** | Agent | 结合 Mem0 用户历史 + 当前测评 |

### 4.3 数据库 Schema 扩展

```sql
-- =====================================================
-- 测评体系 — 新增表
-- =====================================================

-- 题库表
CREATE TABLE IF NOT EXISTS assessment_questions (
    id              SERIAL PRIMARY KEY,
    subject         TEXT NOT NULL,          -- math/english/physics/chemistry/biology/history
    grade_level     TEXT NOT NULL,          -- G5, G6, ..., G12
    topic           TEXT NOT NULL,          -- e.g. "algebra", "quadratics", "reading_comp"
    subtopic        TEXT,                   -- e.g. "factoring", "completing_the_square"
    difficulty      REAL NOT NULL DEFAULT 0.5, -- 0.0 ~ 1.0
    question_type   TEXT NOT NULL,          -- mcq/fill_in/short_answer/essay/experiment
    content_zh      JSONB NOT NULL,         -- { stem, options?, answer?, rubric?, images? }
    content_en      JSONB NOT NULL,         -- 英文版本
    basis_aligned   BOOLEAN DEFAULT TRUE,   -- 是否对齐 BASIS 课程进度
    usage_count     INT DEFAULT 0,          -- 使用次数
    correct_rate    REAL,                   -- 历史正确率
    avg_time_sec    INT,                    -- 平均答题时间(秒)
    tags            TEXT[],                 -- 标签数组
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX idx_aq_subject_grade ON assessment_questions(subject, grade_level);
CREATE INDEX idx_aq_difficulty ON assessment_questions(difficulty);
CREATE INDEX idx_aq_topic ON assessment_questions(topic);

-- 测评会话表
CREATE TABLE IF NOT EXISTS assessment_sessions (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id         INT REFERENCES biz_users(id) ON DELETE SET NULL,
    anonymous_id    TEXT,                   -- 未注册用户的临时ID
    assessment_type TEXT NOT NULL,          -- pre_admission / subject_diagnostic / quick
    subject         TEXT NOT NULL,          -- math/english/physics/...
    grade_level     TEXT NOT NULL,          -- 目标年级
    campus          TEXT,                   -- 目标校区
    status          TEXT NOT NULL DEFAULT 'in_progress', -- in_progress/completed/abandoned
    started_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    completed_at    TIMESTAMPTZ,
    total_questions INT DEFAULT 0,
    correct_count   INT DEFAULT 0,
    final_score     REAL,                  -- 0-100
    ability_level   REAL,                  -- CAT 能力估值 0.0-1.0
    grade_equivalent TEXT,                 -- "At G7 level", "Above G8"
    time_spent_sec  INT DEFAULT 0,
    referral_code   TEXT,                  -- 推荐来源
    utm_source      TEXT,                  -- 营销追踪
    utm_campaign    TEXT,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX idx_as_user ON assessment_sessions(user_id);
CREATE INDEX idx_as_anonymous ON assessment_sessions(anonymous_id);
CREATE INDEX idx_as_status ON assessment_sessions(status);

-- 答题记录表
CREATE TABLE IF NOT EXISTS assessment_answers (
    id              SERIAL PRIMARY KEY,
    session_id      UUID NOT NULL REFERENCES assessment_sessions(id) ON DELETE CASCADE,
    question_id     INT NOT NULL REFERENCES assessment_questions(id),
    question_order  INT NOT NULL,           -- 第几题
    user_answer     JSONB,                  -- 用户答案 (选项/文本/图片)
    is_correct      BOOLEAN,                -- 规则评分结果 (Agent评分的为NULL直到评完)
    score           REAL,                   -- 该题得分 (0-1)
    difficulty_at   REAL,                   -- 答题时的难度级别
    time_spent_sec  INT,                    -- 该题花费时间
    agent_feedback  TEXT,                   -- Agent 对开放题的评语
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX idx_aa_session ON assessment_answers(session_id);

-- 测评报告表
CREATE TABLE IF NOT EXISTS assessment_reports (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    session_id      UUID NOT NULL REFERENCES assessment_sessions(id) ON DELETE CASCADE,
    user_id         INT REFERENCES biz_users(id),
    report_data     JSONB NOT NULL,         -- 完整报告数据 (雷达图数据、分析文本等)
    summary_zh      TEXT,                   -- 中文摘要
    summary_en      TEXT,                   -- 英文摘要
    recommendations JSONB,                  -- 个性化建议列表
    share_token     TEXT UNIQUE,            -- 分享链接的token
    view_count      INT DEFAULT 0,
    is_premium      BOOLEAN DEFAULT FALSE,  -- 是否是付费完整版报告
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX idx_ar_user ON assessment_reports(user_id);
CREATE INDEX idx_ar_share ON assessment_reports(share_token);

-- 学生档案表 (扩展 biz_users)
CREATE TABLE IF NOT EXISTS student_profiles (
    id              SERIAL PRIMARY KEY,
    user_id         INT NOT NULL REFERENCES biz_users(id) ON DELETE CASCADE,
    student_name    TEXT,
    grade_level     TEXT,                   -- 当前年级
    campus          TEXT,                   -- 校区
    target_grade    TEXT,                   -- 目标入学年级 (准新生)
    current_school  TEXT,                   -- 当前学校
    school_type     TEXT,                   -- public/international/private
    enrollment_status TEXT DEFAULT 'prospective', -- prospective/enrolled/alumni
    gpa             REAL,                   -- 当前 GPA
    ap_courses      TEXT[],                 -- 已修/正在修的 AP 课程
    weak_subjects   TEXT[],                 -- 薄弱学科 (由测评自动更新)
    strong_subjects TEXT[],                 -- 强项学科 (由测评自动更新)
    assessment_count INT DEFAULT 0,         -- 完成测评次数
    last_assessed_at TIMESTAMPTZ,
    parent_phone    TEXT,                   -- 家长联系方式
    consultant_id   INT,                    -- 绑定的顾问ID
    tags            TEXT[],                 -- 标签: VIP, probation_risk, etc.
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(user_id)
);
CREATE INDEX idx_sp_campus ON student_profiles(campus);
CREATE INDEX idx_sp_grade ON student_profiles(grade_level);
```

### 4.4 API 设计

```
Business API (:5096) — 新增 Assessment 模块

# 测评落地页
GET  /api/assessment/types                    # 获取可用测评类型列表
GET  /api/assessment/types/{type}/info        # 获取测评详情(题量/时长/说明)

# 开始测评 (无需登录)
POST /api/assessment/start                    # 创建测评会话
     Body: { type, subject, grade_level, campus?, anonymous_id? }
     Resp: { session_id, first_question, total_estimated }

# 答题 (无需登录)
POST /api/assessment/{session_id}/answer      # 提交答案 + 获取下一题
     Body: { question_id, answer, time_spent_sec }
     Resp: { is_correct?, next_question?, progress, is_last }

# 完成测评 → 触发报告生成
POST /api/assessment/{session_id}/complete     # 提交并开始生成报告
     Resp: { report_id, status: "generating" }

# 查看报告 (需注册/登录)
GET  /api/assessment/report/{report_id}        # 获取报告 (需 auth)
     Resp: { report_data, summary, recommendations, ... }

# 报告分享
POST /api/assessment/report/{report_id}/share  # 生成分享链接
     Resp: { share_url, share_token }
GET  /api/assessment/shared/{share_token}      # 公开查看分享报告 (脱敏)

# 学生档案
GET  /api/profile/student                      # 获取当前用户学生档案
PUT  /api/profile/student                      # 更新学生档案
GET  /api/profile/assessments                  # 获取历史测评列表
GET  /api/profile/progress                     # 获取学习进度趋势

# 题库管理 (管理后台)
POST /api/admin/questions                      # 批量导入题目
GET  /api/admin/questions?subject=&grade=      # 查询题目
PUT  /api/admin/questions/{id}                 # 编辑题目
GET  /api/admin/assessment-stats               # 测评数据统计
```

### 4.5 Agent 集成方案

测评系统在两个环节调用 LangGraph Agent：

#### 环节 1：开放题评分

```python
# 在 Business API 的 scoring engine 中
async def score_open_question(question: dict, answer: str, rubric: dict) -> dict:
    """通过 LangGraph Agent 评估开放题"""
    thread = await client.threads.create()
    run = await client.runs.create(
        thread_id=thread["thread_id"],
        assistant_id=ASSESSMENT_ASSISTANT_ID,
        input={
            "messages": [{
                "role": "user",
                "content": f"""作为贝赛思学科测评专家，请根据以下评分标准评估学生的答案。

## 题目
{question['stem']}

## 评分标准 (Rubric)
{json.dumps(rubric, ensure_ascii=False)}

## 学生答案
{answer}

请输出 JSON 格式：
{{"score": 0.0-1.0, "feedback_zh": "...", "feedback_en": "...", "strengths": [...], "weaknesses": [...]}}
"""
            }]
        },
        config={"configurable": {"skill": "student-assessment"}}
    )
    # 解析 Agent 输出
    return parse_scoring_result(run)
```

#### 环节 2：报告生成

```python
async def generate_report(session: AssessmentSession, answers: list) -> dict:
    """通过 LangGraph Agent 生成个性化报告"""
    # 1. 规则引擎先计算结构化数据
    stats = compute_assessment_stats(session, answers)

    # 2. Agent 生成个性化分析
    thread = await client.threads.create()
    run = await client.runs.create(
        thread_id=thread["thread_id"],
        assistant_id=ASSESSMENT_ASSISTANT_ID,
        input={
            "messages": [{
                "role": "user",
                "content": f"""作为贝赛思学术评估专家，请根据以下测评数据生成个性化诊断报告。

## 学生信息
- 目标年级: {session.grade_level}
- 测评学科: {session.subject}
- 目标校区: {session.campus}

## 测评统计
- 总题数: {stats['total']}
- 正确率: {stats['accuracy']:.1%}
- 能力估值: {stats['ability_level']:.2f}
- 年级等效: {stats['grade_equivalent']}

## 各维度得分
{json.dumps(stats['dimension_scores'], ensure_ascii=False)}

## 薄弱知识点
{json.dumps(stats['weak_topics'], ensure_ascii=False)}

## 强项知识点
{json.dumps(stats['strong_topics'], ensure_ascii=False)}

请生成以下内容 (JSON 格式):
1. overall_analysis: 总体分析 (200字，中英双语)
2. dimension_analysis: 每个维度的详细分析
3. recommendations: 个性化学习建议 (3-5 条)
4. study_plan: 4 周学习计划概要
5. next_steps: 下一步建议 (引导到 BasisPilot 辅导功能)
"""
            }]
        },
        config={
            "configurable": {
                "skill": "student-assessment",
                "user_id": session.user_id
            }
        }
    )
    return merge_report(stats, parse_agent_report(run))
```

### 4.6 报告存入 Mem0 用户记忆

```python
# 测评完成后自动存入用户记忆
async def save_assessment_to_memory(user_id: int, report: dict):
    """将测评结果存入 Mem0，供后续辅导对话使用"""
    memory_content = f"""学科测评记录 ({report['date']}):
- 学科: {report['subject']}
- 年级: {report['grade_level']}
- 总分: {report['score']}/100
- 能力等级: {report['grade_equivalent']}
- 薄弱环节: {', '.join(report['weak_topics'])}
- 强项: {', '.join(report['strong_topics'])}
- 建议重点: {report['top_recommendation']}"""

    await mem0_client.add(
        memory_content,
        user_id=f"basis_user_{user_id}",
        metadata={"category": "assessment"}
    )
```

---

## 五、前端页面设计

### 5.1 页面路由

```
/assessment                    → 测评落地页 (营销入口)
/assessment/start              → 选择测评类型 + 信息采集
/assessment/[id]/take          → 答题界面
/assessment/[id]/report        → 测评报告页
/assessment/shared/[token]     → 公开分享报告页
/profile                       → 学生档案 (需登录)
/profile/assessments           → 历史测评列表
```

### 5.2 UI 设计要点

#### 落地页 (`/assessment`)
- 视觉风格：与 Landing Page 一致 (深色科技感)
- 核心信息：3 种测评类型卡片，每种标注题量/时长/适合人群
- CTA：大按钮 "立即免费测评"
- 信任背书：测评维度说明、BASIS 课程对齐标识
- 移动端优先

#### 答题界面 (`/assessment/[id]/take`)
- 顶部：进度条 + 计时器 + 题号
- 中间：题目区域 (支持图片/公式/代码)
- 底部：选项区域 / 输入区域
- 每答完一题有 0.3s 过渡动画
- 答对/答错有即时视觉反馈 (绿色/红色闪)
- 最后 3 题出现 "快完成了！" 激励文案
- 全程无广告干扰

#### 报告页 (`/assessment/[id]/report`)
- 顶部：总分 + 能力等级 (大号数字 + 动画)
- 雷达图：各维度得分可视化
- 年级对齐条：你的水平 vs BASIS G7 要求
- 知识点热力图：按 topic 展示掌握程度
- 个性化建议：3-5 条具体可执行的建议
- CTA 区域："开始针对性辅导" → 跳转 BasisPilot 聊天
- 分享按钮："分享给好友也来测一测"

---

## 六、营销转化设计

### 6.1 投放渠道与引流素材

| 渠道 | 素材类型 | 钩子 |
|------|---------|------|
| **家长微信群** | 测评海报 + 小程序码 | "你家孩子能跟上 BASIS G7 数学吗？免费测一测" |
| **朋友圈广告** | 视频(15s) + 落地页链接 | 对比公立/国际校进度差异 |
| **公众号文章** | 深度文章 + 内嵌测评 | "贝赛思入学数学到底要什么水平？" |
| **教育顾问** | 顾问专属链接(含追踪码) | 初次咨询时发送，量化学生水平 |
| **KOL/家长博主** | 合作内容 + 专属二维码 | 亲测分享 + 裂变码 |
| **学校门口** | 地推传单 + 二维码 | 放学时段精准触达 |

### 6.2 漏斗转化模型

```
投放触达        100%   ← 广告/分享/顾问推荐
    ↓
访问落地页       40%   ← 点击率 (素材质量决定)
    ↓
开始测评         70%   ← 零门槛，不需注册
    ↓
完成测评         85%   ← 自适应 + 进度激励
    ↓
注册查看报告     60%   ← 报告是核心价值交换物
    ↓
查看完整报告     90%   ← 注册后自动跳转
    ↓
点击"开始辅导"   30%   ← 报告内嵌个性化推荐
    ↓
体验课           50%   ← 1 节免费体验课
    ↓
购买课包         70%   ← 效果驱动决策

总漏斗转化: 100 → 40 → 28 → 23.8 → 14.3 → 12.9 → 3.9 → 1.9 → 1.3
即: 每 100 个触达用户 → ~1.3 个付费用户
CAC 估算: 如 CPM=50元, 则 CAC ≈ 50 × 100/40/1.3 ≈ 96 元
```

### 6.3 报告页的转化设计

```
┌──────────────────────────────────────────────────┐
│  📊 你的数学诊断报告                              │
│                                                    │
│  总分: 72/100    能力等级: ★★★☆☆                 │
│  年级对齐: 略低于 BASIS G7 水平                    │
│                                                    │
│  [雷达图：概念理解 85%, 计算能力 78%,              │
│          问题解决 62%, 推理论证 55%]               │
│                                                    │
│  ⚠️ 需要重点提升:                                  │
│  • 问题解决能力 — Word Problems 英文表述理解薄弱   │
│  • 推理论证能力 — 多步推理链不完整                  │
│                                                    │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                    │
│  🎯 专属学习建议                                    │
│                                                    │
│  1. Word Problems 英文表述专项训练 (每周 3 次)     │
│  2. 多步推理题型练习 (Pre-Algebra Ch.5-6)          │
│  3. 学术数学词汇强化 (50 个核心术语)               │
│                                                    │
│  ┌────────────────────────────────────────────┐    │
│  │  🆓 针对你的薄弱环节，BasisPilot 可以帮你：  │    │
│  │                                              │    │
│  │  ✅ AI 1对1 数学辅导 (Word Problems 专项)    │    │
│  │  ✅ 实时答疑 + 步骤拆解                      │    │
│  │  ✅ 每周学习计划自动生成                      │    │
│  │                                              │    │
│  │  [  开始免费体验辅导  ]  ← 主 CTA 按钮       │    │
│  │                                              │    │
│  │  限时优惠: 前 100 名完成测评的用户             │    │
│  │  可免费获得 3 节 AI 辅导课                    │    │
│  └────────────────────────────────────────────┘    │
│                                                    │
│  [📤 分享报告]  [📥 下载 PDF]  [🔄 重新测评]       │
│                                                    │
└──────────────────────────────────────────────────┘
```

---

## 七、学生档案集成

### 7.1 档案数据流

```
测评注册 → 创建 student_profiles 记录
    ↓
测评完成 → 更新 weak_subjects / strong_subjects / assessment_count
    ↓
进入 BasisPilot 聊天 → 自动关联 student_profiles
    ↓
Agent 辅导时 → 读取学生档案 + Mem0 记忆 → 个性化响应
    ↓
每次辅导后 → 更新 Mem0 记忆 + 学习进度
    ↓
定期推送 → 基于档案推荐复测 / 课程
```

### 7.2 档案面板设计

```
┌──────────────────────────────────────────────┐
│  👤 学生档案                                  │
│                                                │
│  ┌──────────────────────────────────────┐     │
│  │ 姓名: 张小明   年级: G7              │     │
│  │ 校区: 深圳贝赛思蛇口                 │     │
│  │ 状态: 在读      GPA: 3.2            │     │
│  └──────────────────────────────────────┘     │
│                                                │
│  📊 学科能力概览                               │
│  ┌──────────────────────────────────────┐     │
│  │  Math     ████████░░ 78%             │     │
│  │  English  ██████░░░░ 62%             │     │
│  │  Physics  ███████░░░ 71%             │     │
│  │  Chemistry████████░░ 80%             │     │
│  │  Biology  █████░░░░░ 55%             │     │
│  └──────────────────────────────────────┘     │
│                                                │
│  📈 测评历史                                   │
│  • 2026-02-21 数学诊断 72分 (G7)              │
│  • 2026-01-15 英语诊断 65分 (G7)              │
│  • 2025-12-01 入学预评估 70分 (G6→G7)         │
│                                                │
│  🎯 当前学习重点                               │
│  • Biology: Cell Structure 专项提升            │
│  • English: Academic Writing                   │
│                                                │
│  [📋 查看完整报告]  [🔄 重新测评]               │
└──────────────────────────────────────────────┘
```

---

## 八、MVP 实施计划

### Phase 1 — 核心测评引擎 (2 周)

| 任务 | 优先级 | 说明 |
|------|--------|------|
| 数据库 schema 创建 | P0 | 5 张新表 + 索引 |
| 题库数据模型 + 导入工具 | P0 | JSON → PostgreSQL 批量导入 |
| 自适应出题算法 | P0 | CAT 简化版规则引擎 |
| 选择题/填空题自动评分 | P0 | 规则引擎 |
| Assessment API (start/answer/complete) | P0 | FastAPI 路由 |
| MVP 题库: 数学 G5-G8 (100题) | P0 | 首批内容 |

### Phase 2 — 前端 + 报告 (2 周)

| 任务 | 优先级 | 说明 |
|------|--------|------|
| 测评落地页 `/assessment` | P0 | 营销入口 |
| 答题界面 `/assessment/[id]/take` | P0 | 自适应答题 UI |
| Agent 报告生成 | P0 | 接入 LangGraph |
| 报告页 `/assessment/[id]/report` | P0 | 雷达图 + 分析 |
| 注册/登录拦截 (查看报告前) | P1 | 转化关键节点 |
| MVP 题库: 英语 G5-G8 (80题) | P1 | 第二批内容 |

### Phase 3 — 档案 + 转化 (2 周)

| 任务 | 优先级 | 说明 |
|------|--------|------|
| 学生档案面板 | P1 | 整合 student_profiles |
| 测评结果 → Mem0 存储 | P1 | 辅导时自动读取 |
| 报告分享 + 裂变 | P1 | 海报生成 + referral |
| 报告 → BasisPilot 聊天引导 | P1 | CTA 跳转 |
| MVP 题库: 理科 G6-G8 (100题) | P2 | 第三批内容 |
| 开放题 Agent 评分 | P2 | 写作/实验设计题 |

### Phase 4 — 优化 + 扩展 (持续)

| 任务 | 优先级 | 说明 |
|------|--------|------|
| 题库扩展到 G9-G12 + AP | P2 | 覆盖全年级 |
| PDF 报告导出 | P2 | 家长需求 |
| 顾问后台 (学生管理) | P3 | B 端功能 |
| 数据分析仪表盘 | P3 | 转化率/题目质量监控 |
| 微信小程序版 | P3 | 移动端触达 |

---

## 九、核心指标 (KPI)

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 测评完成率 | > 80% | 开始测评 → 完成 |
| 注册转化率 | > 55% | 完成测评 → 注册 |
| 报告查看率 | > 90% | 注册 → 查看报告 |
| 辅导引导点击率 | > 25% | 报告 → 点击"开始辅导" |
| 平均答题时间 | < 15 分钟 | 不要让用户等太久 |
| 分享率 | > 10% | 报告 → 分享给好友 |
| 周新增测评 | > 100 | 稳定获客 |
| 月活用户 | > 500 | 3 个月后目标 |

---

## 附录

### A. 可视化原型

详见: `docs/免费学科测评/assessment-prototype.html`

### B. 参考资料

- [BASIS 课程体系](https://basisindependent.com/academics/curriculum/)
- [BASIS 课程 FAQ](https://blog.basisinternationalschools.com/faq-about-the-basis-curriculum/)
- [贝赛思中国官网](https://www.basischina.com/)
- [MAP Growth Assessment](https://www.nwea.org/map-growth/)
- [Computer Adaptive Testing](https://en.wikipedia.org/wiki/Computerized_adaptive_testing)
